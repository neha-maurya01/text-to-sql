"""
Defines constants for the database connection.

This module contains two constants, `DB_HOST` and `DB_NAME`, which specify
the host and name of the database, respectively. These constants are used
to configure the database connection in other parts of the application.

Constants:
    DB_HOST (str): The host address of the database.
    DB_NAME (str): The name of the database.
"""
DB_HOST = "localhost"
DB_NAME = "gemini_demo"